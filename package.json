{
  "name": "rise",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "R.I.S.E. - Recursive Installation of Symbolic Experience",
  "engines": {
    "node": ">=20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "build:pericopes": "node scripts/build-pericopes.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^26.0.0",
    "vite": "^7.2.4",
    "vitest": "^3.0.0"
  }
}
