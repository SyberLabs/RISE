# Roadmap — session of 2026-08-04

*Written after the mobile threshold work shipped and SOL's Experience Program
phases 1–4 were committed (`0fb9c2b`). This sequences what is actually open,
says why in that order, and names what is deliberately not in it.*

Specs are cited, not restated. Where a line says "per X", X is the authority
and this document adds nothing to it.

The continuous acquisition → Experience Program → Workshop → Page thread is
closed out in `SESSION-2026-08-11-ARCHIVE-WORKSHOP-PAGE.md`. It records the
implementation sequence, verification, Karenina diagnostic, and the exact
boundary of the Page work parked below.

---

## Where we are — 2026-08-11

**22 of 40 items, 55%.** The counting convention: phases 8–13 arrived after the
original 24-item tally and were never folded into it, so this table covers every
phase the document names and counts items as its own sections name them. Phase 4
is counted by its three lanes rather than as one item.

| phase | items | done | state |
|---|---|---|---|
| 0 · Ground the instruments | 3 | 3 | **complete** |
| 1 · The Page paginates | 4 | 4 | **complete** |
| 1b · Page Mode comprehensive pass | 4 | 4 | **complete** |
| 1c · Reading onward | 1 | 0 | scoped, not started |
| 1d · Static authored visual parity | 1 | 1 | **complete** |
| 2 · The Stream's seams | 2 | 1 | 2.1 shipped; 2.2 specced |
| 3 · Experience Program 5 → 7 | 4 | 3 | **3.1, 3.2 and 3.4 shipped**; 3.3 partial |
| 4 · RISE Chain | 3 | 1 | text lane shipped as a sandbox; visual + audio open |
| 5 · The Solarium, narrowed | 1 | 0 | its gate (3.2) is now open |
| 6 · The Atrium on a phone | 1 | 0 | needs a design decision first |
| 7 · The chunker's punctuation | 3 | 1 | 7.1 shipped; 7.2 and 7.3 open |
| 8 · The vertically responsive band | 1 | 1 | **complete** |
| 9 · A book's own plates | 1 | 0 | measured, undecided where a figure appears |
| 10 · Apparatus in the reading stream | 3 | 2 | A and B applied; the identity guard open |
| 11 · Atrium passage additions | 1 | 0 | an acquisition, not an edit |
| 12 · Two external reviews | 5 | 1 | 12.1 shipped; 12.2–12.5 open |
| 13 · A program that can score the reading | 2 | 2 | **complete** — pacing shipped, engine descriptions written |

**Phase 0.4 is closed but for its one decision.** Findings 2, 3 and 4 all landed:
the limits are one authority (`READING_LIMITS`, read by both
`workshop-project.js` and `session-compiler.js`), Workshop media is durable
rather than a data URI in `localStorage`, and same-lane overlap is refused
inside `experience-program.js` rather than in the editor adapters alone. Only
finding 1 — War's coda — remains open, and it remains a decision.

**The Live Curator's boundary is open.** 3.2 shipped: programs serialize,
parse, import against a context, and refuse with a copyable explanation.
3.4 shipped as `WORK_ENGINE_MANIFEST` + `visual-registry.js`, checked against
the engine modules. What 3.3 still lacks is a written API contract — the
Scriptorium is currently its only documentation.

### Shipped 2026-08-11

- **The Workshop's post-spec hardening pass.** Visual, Audio, and Combined
  authoring now share one selection model; the Inspector is a persistent
  source-ordered sequence map with bidirectional passage correspondence; the
  redundant preview rail is gone; source/project guidance reacts immediately;
  and the asset surface follows Even Design, hierarchical style controls, and
  a single numeric Gallery cadence. `Off` remains an internal stillness cue,
  not an assignable asset.

- **Persistent visual fields became passage cues.** Focal, Genesis, and
  Attractor now compile as bounded `field` cues and execute through one
  lifecycle director. Passage authority temporarily supersedes the scored
  fallback without splitting the Session or copying WPM/chunking state. Klee,
  Harmonograph, Genesis, Attractor, and Focal configuration use shared bounded
  definitions. Personal Focals can reuse project media or import directly and
  retain the focal's small rectangular composition.

- **The Karenina correction.** A phrase atom could cross several adjacent
  visual clips, so the first cue won, an intermediate cue never became active,
  and the next cue appeared late. Authored media endpoints now become hard
  atomization cuts, DOM selections snap outward to complete tokens, and
  compilation refuses an atom carrying two clips from one track. The exact
  Chapter 1 regression is pinned in `source-span.test.js`. Separately, the
  book's title/Contents/all-eight-parts preamble is recorded as Archive
  apparatus in `ARCHIVE-CLEANSING-SPEC` §2g; cleansing has not been implied by
  the runtime fix.

- **Workshop → Page static visual parity.** Direct Page launch preserves
  authorship while deferring temporal presenters; whole-reading/fallback and
  passage focals compose at their correct spatial boundary; project focal
  images resolve from the Session registry. The full boundary is proven from
  Workshop registry through project hydration and Session compilation to Page
  DOM. Dynamic scored fields and video posters remain the separate parked
  projection phase below.

- **The Scriptorium's length control.** One dial beside intent: how long the
  reading should be, in **words**, carried as `constraints.targetWords` and
  enforced as a hard refusal at the gate. It replaces `targetMinutes`, a field
  that had existed since the context schema was written and was populated by
  nothing, read by nothing, and mentioned in no prompt.

  Words rather than minutes because a model can add words up from the library
  it was handed, because the ceiling that actually refuses is `maxAtoms` and
  one word is one atom in word chunking, and because Phase 13 made duration a
  function of the score — a minute budget could not be checked until after the
  thing being budgeted had been composed. Minutes are shown under the slider,
  derived at the reader's wpm, never stored.

  The measurement is exact: a movement anchor carries only `sourceIds`, so a
  movement reads its source whole and a score's length is the sum of `words`
  over the sources its movements name. A source of unknown length refuses the
  score rather than being admitted unmeasured.

  `maxAtoms` moved into `READING_LIMITS`, which says in its own header that
  every reading-size ceiling belongs there (Phase 0.4 finding #2).

### Shipped 2026-08-10

- **Phase 13, a program that can score the reading** — see that section.
- **Phase 8, the reading band** — see that section for the rulings.
- **The Scriptorium can be reached to its last step.** It set `height: 100%`
  against a parent with no height under an `overflow: hidden` body, so it
  established no scroll context and its verdicts rendered below the fold.
- **The photosensitivity gate moved one level deeper.** It fired on selecting
  Rhythmic, which then defaulted to Full Frame — so a reader met a warning for
  a surface they had not chosen. It now fires on **Behind Stream and Full
  Frame** only, keyed on `[data-presentation]` rather than `[data-visual-mode]`,
  and Gallery — the default, which never flashes — never prompts.
  `beginNonFlashingVisualSession` is deliberately separate from the accepting
  path, so a non-flashing session cannot consume an acceptance it never needed.
- **The e2e server no longer reuses a listener on 4317.** Reuse also skipped
  the build command, so a stale preview served an old `dist` and the suite
  reported on code that was not in the tree. It cost two wrong conclusions in
  one day, including making a correct fix appear to fail.

### Recorded from 2026-08-05, unchanged

**SCIENCE IMAGERY IS COMPLETE AND DEPLOYED** — the largest single piece of work
of 2026-08-05, and it was a *parked* entry rather than a numbered phase, which
is why the table above does not show it. The parked entry said three narrow
gaps remained. All three closed, and the item went the whole distance:

- Licence classes that keep opposite obligations apart — `OPEN`, `BY`, `BY_SA`,
  `PD_CREDIT` (which NASA needed and did not exist), `UNDECLARED` — with the CC0
  corpus provably untouched by any of it.
- A work that cannot be credited is withheld; a credit is composed once at the
  boundary and travels with the work.
- Two rulings, both implemented: the credit lives as text in the chip with the
  full record in the Curia, and an appended roster is cut at its marker rather
  than at a length.
- Harvest → contact sheet → pin → catalogue → provider → panel, end to end.
  **216 astronomy works** (ESA/Hubble 98, NASA 64, ESO 54); Smithsonian reviewed
  entire and retired, its 54 refusals recorded so the next harvest cannot
  re-propose them.
- `Science Collections` in the Visual panel, and `Art Institute Collection`
  renamed `Museum Collections` — a heading outvoted by the Rijksmuseum in five
  of its own categories.

**Done off-roadmap 2026-08-05, because using the thing found it:** the Art
Institute rejects any loopback `Referer` with a 403, which blackened
`aic-ukiyoe` (the one category with no institution to fall back on) and made
five others degrade silently. Fixed by sending no referrer at all, which is also
the right privacy posture. Two new standing guards came out of the same pass —
every panel category must route to a provider that can serve it, and every
section of a Gospel chapter must carry imagery distinct from its neighbours.
The suite had been checking that pericopes are disjoint *in verses* and never
that they differ *in imagery*.

**The critical path to the thing actually wanted:** RISE Chain is gated on
**nothing in Phase 3** — re-examined 2026-08-05, see *Phase 4 prerequisites*.
Its one real dependency is a parameter surface on a single soundscape, which is
the sandbox's own first component. Everything in Phase 3 is the Live Curator's
path, not the Chain's.

**Open decisions blocking work** — listed at the foot of this document and one
at the foot of `ARCHIVE-CLEANSING-SPEC`.

---

## The shape of it

Six phases, in dependency order, plus a parked list. The ordering principle is
simple and worth stating once: **repair what everything else is measured with,
then spend the session on the one item that unblocks the most, then take the
highest-leverage single unlock, then build the thing we actually want.**

| phase | what | why here |
|---|---|---|
| 0 | Ground the instruments | every claim rests on suites that are now degrading |
| 1 | The Page paginates | gates procedural figures, which are already built |
| 2 | The Stream's seams | one decided deferral, one new idea; both are boundary behaviour |
| 3 | Experience Program 5 → 7 | step 6 alone enables the Live Curator |
| 4 | RISE Chain | the point of the exercise; **gated on nothing in Phase 3** (see Phase 4 prerequisites) |
| 5 | The Solarium, narrowed | newly cheap now that the Experience Program is canonical |
| 6 | The Atrium on a phone | the one surface the mobile pass never reached |
| 7 | The chunker's punctuation | touches every reading; the only item with no prerequisite |

---

## Phase 0 — Ground the instruments

> **DONE, 2026-08-04.** `npm run test:run` is green with no flags (110 files,
> 1484 tests, ~96s) and the full e2e suite exits 0 for the first time today
> (63 passed, 2 skipped by design). Three timing defects were found and fixed,
> and the round-trip suite was mutation-tested against the original bug.


**Rationale.** The unit runner now crashes its worker pool outright
(`ERR_IPC_CHANNEL_CLOSED`) and only `--no-file-parallelism` yields a trustworthy
tally; `recitation.spec.js › an uncovered reading is read silently rather than
stalled` fails under full-suite load and passes 3/3 alone. Neither is a product
fault. Both mean the thing we verify with is less reliable than the thing we are
verifying, and this session is about to add a persistence format, a media type
and a sandbox — three areas where a false green is expensive.

**0.1 — Make the unit suite survive itself.**
Configure the pool in `vitest.config` rather than relying on a flag nobody will
remember. 109 files and 1477 tests is the new floor; it will keep growing.

**0.2 — Stabilise or quarantine the recitation flake.**
Find the timing assumption. If it cannot be made deterministic this session,
quarantine it *by name and with the reason written down* — a skipped test whose
skip is unexplained is the same failure as an inert guard.

**0.3 — The lifecycle round-trip suite.**
The cheap alternative to the virtual-clock kernel, named when that kernel was
deferred: assert that every identity-carrying config field — `visualProgram`,
`sourced`, `focal`, `atriumCollections`, and now the canonical Experience
Program — **survives or dies correctly** across persist → restore → reset.

This would have caught the entire Matthew-27 bug class. It is also the natural
place to *formally close* that item: `deserializeVisualProgram` is now in
`ChamberOrbital`, so SOL's fix appears landed, and this suite is how we prove it
rather than assume it.

**Exit:** one command, no flags, green, with the round-trip suite in it.

---

## Phase 1 — The Page paginates

**Rationale.** It is the largest unlock available and the only item on the list
that other work is explicitly waiting behind.

**1.1 — Pagination**, per `docs/vision/PAGE-MODE-SPEC.md`. Page Mode was always
intended to paginate; the current Page is one continuous column, which is also
why the whole reading's overhead lands at once instead of in divisible chunks.

**1.2 — Turn on procedural figures.** Already built and tested —
`pageCollectionId` and `sampleWorkEngine` in `work-engines.js`, the Chamber's
resolver branch — and held behind one constant, `PROCEDURAL_FIGURES` in
`flow.js`. Per `EXPERIENCE-PROGRAM-SPEC.md` §12 this was deferred *specifically
until the Page paginates*, so it lands here and costs almost nothing.

**This is the Journey's remaining Page gap.** A Journey's *museum* works already
reach the page — that fix landed. What a Journey's page still lacks is its
PROCEDURAL imagery, which is this item and not a separate one. Worth stating
because "Journey Page Mode support" reads like a whole feature and is in fact
one constant behind one dependency.

**1.3 — Projection is not carried through persistence** (red-team #11). A
restored session forgets whether it was a page or a stream.

> **Shipped 2026-08-11.** Projection now travels with the saved reading
> identity, restores on reconstruction/reset, and a newly loaded reading
> starts in Stream rather than inheriting the prior Page choice.

**1.4 — Print output depends on scroll position** (red-team #12). Figures
hydrate lazily via `IntersectionObserver` in `PageReader.js`, so printing before
scrolling omits every figure never brought into view. Any fix must force-hydrate
before print. Note for whoever writes the test: this same property is why a test
counting `.page-figure img` must scroll first or it undercounts — it has already
cost real debugging time once.

> **Shipped 2026-08-11.** Print preparation materializes the entire composition,
> disconnects lazy observation, and eagerly resolves every figure; `afterprint`
> restores the exact interactive page/projection/scroll state.

**Not in this phase:** the Journey's Page support, which **already landed**. The
Page now reads a Journey's authored program; the Homeric movement's museum works
reach the page.

---

## Phase 1b — Page Mode: the comprehensive pass *(new, 2026-08-04)*

> **DONE, 2026-08-04.** All four items landed. The unit suite is green and
> `e2e/page-typography.spec.js` verifies the results in a real browser.

Requested after reading real pages. Pagination shipped and then accumulated
fixes fast enough that the rules began contradicting each other; this is the
pass that makes them one system rather than a pile.

**1b.1 — The known gap. SOLVED, and it was not a rule at all.** An inline
`CHAPTER II` did not open a page. `opensAChapter` was reached, its inputs were
correct, and it still returned false — because the word boundary in its regex
had been written to disk as a **literal BACKSPACE (U+0008)** instead of the two
characters backslash-b, so the pattern demanded a control character after
"chapter". The byte is invisible in an editor, in `git diff`, and in every file
dump used to hunt it; two sessions of reasoning about rule ordering found
nothing because there was nothing there to find. It was located by printing char
codes.

A standing guard now sweeps every source file for control characters
(`src/core/source-hygiene.test.js`), because a defect nobody can *see* is worth
one cheap check per run. It was the only occurrence in the repository.

**1b.2 — Rule conflicts, resolved deliberately. DONE.** The corrective passes
now have a stated precedence rather than an accidental order — **structure
outranks balance, balance runs last and may not violate structure** — written
into `paginator.js` where the passes are. Three real conflicts were found and
each has a test:

- the balance pass buried a chapter opening under borrowed prose;
- the heading pass could strand a page holding a single item;
- the balance pass's own cascade walked a thin page from the tail of a reading
  **all the way to page one**, where the reader meets it first. The existing R4
  test inspected only the LAST page and watched it stroll past.

Where a loan would merely relocate the fault, the thin page is now absorbed
upward within a tolerance the frame already allows.

**1b.3 — Elongate. DONE, and the fault was worse than "not findable".** The
control *hid itself* once pressed: `PageReader` reported four positional
arguments and the Chamber took the first two, inferring the rest from `total` —
and an elongated reading is ONE page, which reads as "nothing to paginate". The
report is one **object** now, so a field cannot be dropped the way a trailing
argument can. Elongating also **keeps the reader's place** in both directions,
anchored on an item rather than an offset.

**1b.4 — Verify against readings, not fixtures. DONE.** Two heading fixes passed
unit tests written to match my own model of the fault while the page stayed
visibly wrong. The instrument that found it is now
`e2e/page-typography.spec.js`: it walks the real pages and asks a question about
GEOMETRY — is a figure sitting level with a title while standing off to one side
of it — which is the signature of a float that has wrapped a heading. It also
verifies the chapter opening and the Elongate round trip in a browser.

---

## Phase 1c — Reading onward *(new, 2026-08-04)*

**A gap in the UI model, not a bug.** A reader who finishes a chapter cannot
advance to the next without returning to the Chamber. The Page is a projection
of a session, and the session is one reading — so "next chapter" is not a Page
control at all: it must move the STREAM's position too, or the two projections
disagree about where the reader is, which is the one thing this architecture
exists to prevent.

That makes it a session-level act with a Page affordance, not a page-turn. Worth
its own design pass; noted here so it is not rediscovered.

---

## Phase 1d — Static authored visual parity *(shipped 2026-08-11)*

Workshop scores now retain their static visual meaning across Stream and Page.
A direct Page launch preserves the authored visual configuration while deferring
temporal presenters; leaving Page activates consent/preload and constructs the
Stream surface rather than restoring from a shadow `suspendedVisualMode`.

The Page holds a whole-reading or program-fallback focal once in its masthead,
places passage focal cues at their stable source-span boundary, and resolves
project images from the session asset registry instead of an external provider.
The lifecycle suite proves Workshop registry → canonical project → hydration →
Session compilation → Page DOM for a personal passage focal.

**Deliberately parked:** cue-specific parametric sampling for scored Genesis and
Attractor configurations, and static MP4 poster extraction. Whole-reading
dynamic modes currently have one configuration to sample; a score can carry
several. The next Page phase therefore needs a config-keyed, abortable sampler,
bounded cache and per-reading sample budget rather than configuration encoded in
collection ids. MP4 needs a deterministic poster extraction/storage contract;
Page must not run temporal video merely to discover a frame. These are dynamic
projection features, not omissions from this static parity correction.

---

## Phase 2 — The Stream's seams

Two boundary behaviours: what the imagery does when the reader stops, and what
the reading shows when one work ends and another begins.

### 2.1 — The Gallery pause boundary

**Correction, 2026-08-04.** This was filed under "Page Mode open threads" and it
is a **Stream** question — Gallery is a continuous field behind the reading
stream. It appeared in the Page notes only because it surfaced during that
hardening pass.

**The rule is decided:** an authored program **pins** the imagery to the
reading, an unauthored pool does not. So pausing halts the gallery when a
`visualProgram` is driving it — otherwise the imagery plays ahead of the text
and then runs out of hydration — and lets it drift for procedural and general
collections, where it is ambient and has no claim on any particular passage.

This is the same bound/unbound distinction `cueForAtom` already encodes, which
is the argument for it: no new vocabulary, one existing question asked in one
more place.

Small, decided, and it closes a deferral rather than opening one.

> **Shipped 2026-08-11.** Both Gallery presenters preserve their exact layers,
> shuffle/cadence state, and remaining dwell across pause/resume when an authored
> `visualProgram` owns the clock. An unbound ambient Gallery keeps drifting.

### 2.2 — The intermission between movements

*New, Mateo, 2026-08-04.* When a Journey's movement ends, hold a brief
intermission — smooth transitions, and the title and details of the work coming
next.

**It has a home already, which is the argument for building it now.** Three
pieces exist and none of them were built for this:

- A **boundary is an atom**, not a timer (`journey-schedulers.js`). "The
  transition IS the current atom, so pausing a Journey pauses its transitions
  for free, and no controller can drift from the state." An intermission built
  on this inherits pause, resume, scrub and shuttle without writing any of them.
- The canonical Experience Program already carries **transition** as an
  independent track beside movement, visual, audio-bed and swell. The
  intermission is that track becoming visible; it is not a new concept.
- The audio schedule already **fades to silence at a V1 boundary**. So the seam
  is currently full of nothing in both channels — the intermission fills one of
  them and deliberately leaves the other empty.

**It revises a decision that was made on purpose,** and that should be done with
open eyes rather than by overwriting a comment. `Chamber.onMovementChange`
currently handles `kind: 'boundary'` by announcing *nothing*, and says why: *"A
transition announces nothing. It is the silence between two worlds, and naming
it would be talking over it."*

That reasoning still holds for naming **the transition**. It does not hold for
naming **what is arriving** — the difference between captioning a silence and
using it to introduce the next work. The build should keep the first prohibition
and take the second liberty, and the comment should be rewritten to say so.

**The three-layer law decides the content.** Per `JOURNEYS-SPEC` §5 the Chamber
receives a label and an identity, never a meaning: it must not compose the
intermission. The **Journey authors** what the card says, the runtime schedules
it on the boundary atom, the Chamber renders what it is handed. Movements
already carry the material — the credits line the Journeys view shows ("The
Storm of Steel — Chatto & Windus, London, 1929; Basil Creighton translation") is
authored per movement and is exactly the "details" being asked for.

**Design constraints, so it stays a threshold and not a splash screen:**

- **Duration is authored, not hard-coded.** The boundary atom's own duration
  governs the hold, so "brief" is a compositional decision per Journey.
- **Reverent degradation.** No credits → title alone. No title → today's
  silence, unchanged. The intermission never becomes a broken frame.
- **Reduced motion.** The crossfade is a state change, not an animation.
- **The phone.** It renders inside the reading band's grammar — the same
  stratum, not a new full-screen surface.
- **Silence stays silent.** The audio bed's existing fade at a boundary is not
  disturbed; the intermission is visual only.

**Sequenced after 2.1** because 2.1 is decided and small, and because both
touch what happens at a boundary — doing them together means reading that code
once.

Worth its own short spec section before building. It is the first thing in a
while that adds a *surface* rather than correcting one.

---

## Phase 3 — Experience Program, steps 5 → 7

Per `docs/vision/EXPERIENCE-PROGRAM-SPEC.md` §build-order. Steps 1–4 shipped in
`0fb9c2b`.

**3.1 — Step 5: MP4 as a first-class visual asset.** The spec already draws the
line and it should be honoured exactly: MP4 **as an imported media object**
belongs in the Workshop today; **exporting a program as MP4** is a
rendering/distribution pipeline and stays deferred by the North Star. Only the
first half is in scope.

> **Shipped 2026-08-11.** Sequence-local MP4s are durable Workshop assets with
> probed duration, strict muted/poster policy, canonical video cues, passage
> assignment and persistence. Runtime playback exists only under active cue
> authority, pauses with the reading, cancels on replacement/Page projection,
> and fails to stillness. Full-frame layout measures media/stage aspect and
> switches from cover to contain when a crop would discard too much of the
> authored frame. Program-to-MP4 export remains deferred.

**3.2 — Step 6: export/import Experience Program JSON.** The single
highest-leverage item on the whole list — the spec's own words are that this
*"alone enables the Live Curator."* The format is now canonical, strict and
immutable, so this is a boundary to open, not a design to invent.

**3.3 — Live Curator API documentation.** Follows 3.2 immediately: the moment
the format can leave the building, the contract for it has to be written down.

**3.4 — Step 7: formalise engine registry scope.** Small on its own, and it is
Phase 4's prerequisite, so it lands at the end of this phase rather than the
start of the next.

**Deliberately deferred:** step 8, the declarative Procedural Recipe format. It
is real work with its own design surface, it sits between "parameterised native
engines" and "never unrestricted same-origin JavaScript" on the spec's security
ladder, and RISE Chain does not need it to begin.

---

## Phase 0.4 — the canonical boundary, end to end *(external review, verified)*

An outside review of `0fb9c2b` raised five findings. **I checked all five
against the code rather than accepting them, and all five are real.** That is a
good review. Two corrections to its framing are noted below.

| # | finding | verified | state |
|---|---|---|---|
| 5 | a named swell substitutes a random one | `engine.js:1156` | **FIXED 2026-08-05** |
| 2 | Workshop and Session limits disagree | 4,194,304 vs 2,000,000 | open |
| 3 | accepted media cannot be stored | 8 MB → data URI → `localStorage` | open |
| 4 | canonical validation permits same-lane overlap | no `overlap` in `experience-program.js` | open |
| 1 | War's coda never executes | `handoff.test.js` "drops the coda" | open, **by decision** |

### 5 — fixed, because it broke a standing law

`playSwell('funeral-bell')` with nothing by that name warned and then played a
**random different swell**. The score asks for a funeral bell; the runtime
sounds a rising cosmic swell, and nothing says so.

That is not graceful degradation, it is **false execution**, and it violates the
rule the imagery has followed from the beginning: *a work that will not resolve
is absent, never a substitute.* Silence is a valid scored state; the wrong sound
is not. A named request now fails closed. An unnamed one — *"give me a swell"* —
is a different request and still takes any of them.

The test observes whether a buffer source was **created**. A throwing stub
proves nothing here: `playSwell` wraps its body in try/catch and swallows it.

### 2, 3, 4 — open, and the review understates 3

**2.** `workshop-project.js` allows 4,194,304 characters per source;
`session-compiler.js` allows 2,000,000 per source *and* in total. A project
validates, saves, reopens — and fails only at Run. One shared limits authority,
and an invariant: *every valid Workshop project compiles.*

**3.** Worse than stated. An 8 MB image becomes a data URI, which base64
inflates by about a third — roughly 10.7 MB — and the whole project is then
`JSON.stringify`d into `localStorage`, whose typical ceiling is 5–10 MB for
*everything*. **A single accepted image cannot be saved at all.** This is a hard
prerequisite for MP4, not a parallel concern.

**4.** Confirmed: `experience-program.js` contains no overlap check. The rule
lives in the editor adapters alone — which is this codebase's oldest failure
shape, a vocabulary in two places where only one copy is maintained. Array
order must not become an accidental layer system. **The JSON import boundary
should not open until this is canonical.**

### 1 — real as a product gap, but it is a DECISION, not an oversight

The review reads it as a defect. The test is named *"drops the coda, which joins
nothing"* and says why: *"A transition after the final movement is a scored
ending. It stays in the movement program for reporting and has no atom, because
there is no break for it to replace."* That is the review's own option 2,
chosen deliberately.

**Its argument for option 1 is still the better one.** War ends in an emptied
landscape and silence, and authors seven seconds of exactly that. A Journey
should finish in its authored silence. Worth revisiting on those grounds — not
as a bug report.

### Where the review is overtaken

It predates this session's work. Page Mode **is** paginated, Phase 0 is complete,
and 3.4 does not gate the Chain (below). Its sequencing advice — durable asset
storage before MP4, canonical overlap validation before JSON import — stands.

---

## Phase 4 prerequisites — re-examined 2026-08-05

**3.4 does not gate RISE Chain, and the argument is in the build order itself.**

`EXPERIENCE-PROGRAM-SPEC` step 9 specifies the Chain as *"a research mode —
sandbox, **one visual engine, one soundscape**, before any Journey
integration."* §6's engine registry scope solves a **browsing and visibility**
problem: keeping "Michael's Flaming Sword" out of a menu beside "Fractal Flame"
without a second allowlist. **One engine means no menu to filter.** The
taxonomy is real work and it belongs to the Workshop and the Live Curator, not
to a sandbox that instantiates a single engine by name.

Dropped from the critical path. Phase 4 is gated on nothing in Phase 3.

### What the Chain actually needs, read off its own §9

```
generated phrase → shifts visual dynamics → changes acoustic state
    → alters transition probabilities → conditions the next phrase
```

Four coupled processes. So the question is not "what taxonomy do we have" but
**"can each layer be driven, continuously, at runtime?"** Measured:

| layer | surface today | ready? |
|---|---|---|
| visual | `generate(signal, seed, options)` and `step(dt, signal)` — every engine takes a **`signal`** on every frame | **yes** |
| audio | `createSoundscape(id) → { start, stop }` | **no — binary** |

The visual half is already built and the parameter is even named `signal`,
which is the right word for it. A soundscape has no equivalent: it is on or
off, and there is no way to say *more agitated*.

### On binding audio to the Experience Program

Mateo's instinct — that the audio layer is the missing piece — is right in
substance, and the fix is not the one it sounds like.

**Binding audio to the Experience Program is already done.** Build-order step 4,
"the audio lane and sync groups", landed 2026-08-04: independent bed/event
lowering and execution, tone cues, sync-group preservation, transport
cancellation.

**And it is the wrong instrument for this anyway.** The Experience Program is a
*canonical authored score* — compiled ahead of time, immutable, never
second-guessed. The Chain is generative **at runtime**. A score written in
advance cannot express *"the acoustic state changed because of the phrase just
generated"*, because that phrase did not exist when the score was written. A
better score does not help; the Chain needs the thing a score deliberately is
not.

### The one real prerequisite, and it is small

**Give one soundscape a parameter surface**, matching what visual engines
already have — `{ start, stop, signal(state) }` or equivalent. One soundscape,
because that is what step 9 asks for.

That is the whole gate. It is smaller than 3.4 and it is the sandbox's own
first component rather than a dependency in front of it.

---

## Phase 4 — RISE Chain

Step 9, per the spec: a **research mode** — sandboxed, one visual engine.

### 4.1 — The text lane — **STATUS: SHIPPED (sandbox), 2026-08-05**

Not wired into the Chamber and not meant to be yet. `src/chain/markov.js`
+ `scripts/chain-sandbox.mjs` + `scripts/chain-profiles.mjs`, 21 unit
tests. What exists: variable-order chain over the whole shelf, entropy
backoff, token-level verbatim ceiling, seeded replay, per-transition
genealogy, selection profiles, and the `bias` hook the coupled organism
will reach through.

**Open in this lane:** per-step modulation of `tail:k` (the natural
bridge to the visual lane), sentence-boundary awareness so a dream ends
on a full stop, and the Chamber surface itself. Parked deliberately —
§9's other two lanes come first, and audio wants its own day.

`src/chain/markov.js` — a variable-order Markov chain over the Archive,
with `scripts/chain-sandbox.mjs` to hear it. Text only: no visuals, no
audio, nothing wired into the Chamber. Audio is deliberately deferred to
its own day of engineering, per Mateo — the Chain's sound wants to be a
live instrument the chain is playing, not a soundscape it triggers.

**The one design decision worth carrying forward.** A fixed order-3 chain
does not *sometimes* quote; it quotes whenever its context is
deterministic, and in natural text most trigrams have exactly one
continuation. Raising the order makes it strictly worse — at order 5 the
chain is a photocopier. So the model measures the **entropy** of the
context it stands in and backs off to a shorter one when the
distribution collapses. §9's law and information theory turn out to be
the same constraint: *a deterministic context carries no information, and
following it is transcription rather than composition.* It is therefore
implemented once, in the sampler, rather than as a filter afterwards.

A hard ceiling backs it up, and it is a constraint on the TOKEN rather
than on the order — the first attempt backed off to a shorter context,
which bounds nothing, because order 0 is unguarded by construction.

**Measured over the whole shelf:** 88 works, 830,887 tokens, order 3.
Longest single-work run 3–5 tokens; genealogy diffuse, no work above 7%.
The property is worth naming: **the more of the Archive it dreams over,
the less any one book owns a sentence,** and the verbatim guard barely
has to fire.

**Next in this lane:** the `bias` hook already exists — `(token, context)
→ multiplier` — which is where §9's coupled organism will reach in. The
visual lane conditions the text by reweighting transitions, never by
editing output.

**Gated on nothing in Phase 3.** This paragraph used to say "gated only on step
7 (3.4)" and that is wrong; see *Phase 4 prerequisites* above for the argument,
which comes from the build order itself. The one real prerequisite is a
**parameter surface on one soundscape**, and that is the sandbox's own first
component rather than a dependency in front of it.

*(Corrected 2026-08-05. The contradiction stood in this file for one commit —
the new section said one thing and this one still said the other. A roadmap is
architectural memory for later sessions, and two critical paths in one document
is worse there than ordinary stale prose.)*

---

## Phase 7 — The chunker's punctuation *(new, 2026-08-05)*

> **SHIPPED 2026-08-05** (`04e0be2`). Phrase mode now breaks at `?` and
> `!`, allows a closing quote between the mark and the space, and treats
> a parenthetical as one breath — split at its edges, never within.
> Measured before and after through the real `chunkText` on eight works
> chosen for the genres the awareness study warned about.
>
> | work | atoms | frag% | internal `?` | unbalanced `( )` |
> |---|---|---|---|---|
> | meditations | 1149→1139 | 15.1→**14.0** | 52→**0** | 12→**2** |
> | a-doll's-house | 2660→2763 | 48.8→48.6 | 81→**5** | 0→0 |
> | paradise-lost | 2618→2631 | 0.3→0.6 | 30→4 | 24→**4** |
> | walden | 1324→1336 | 8.1→8.0 | 21→4 | 0→0 |
> | moby-dick | 812→815 | 6.5→6.7 | 7→**0** | 4→**0** |
>
> **Three findings worth keeping.**
>
> 1. **`?` and `!` need no capital guard.** That guard exists for
>    "Dr. Smith"; no abbreviation ends in a question mark. It mattered —
>    this translation continues in lower case (*"…to do me hurt? for what
>    profit…"*), which even SENTENCE mode was missing.
> 2. **A closing mark may stand between the punctuation and the space.**
>    `…I conclude?”` — sixteen such lines in three chapters of Pride and
>    Prejudice. `applyPhraseFloor` already allowed for this in
>    `closesSentence`; the splitter did not, so the two disagreed about
>    where a sentence ended.
> 3. **An authored paragraph keeps its own phrasing.** The floor already
>    declines to MERGE across a hand-placed `|`; adding a break inside one
>    is the same overreach reversed. Parenthetical edges are not promoted
>    in a paragraph that carries pipes, though the interior is still
>    protected.
>
> **Knock-on, handled rather than absorbed.** 37 Atrium browse durations
> moved and were regenerated from the same code path the test pins. One
> journey crossed an editorial ceiling asserted to the millisecond;
> rather than widen it to whatever passed, the shift was measured (mean
> **0.10%**, largest 0.72%, and that journey already sat at 17.98
> minutes) and the ceiling is now a named constant with a tolerance fifty
> times the mean — so a 5% move still fails loudly.
>
> **STILL OPEN:** 7.2 below, and 7.3 the parser item.

### 7.1 — Phrase mode — SHIPPED

### 7.2 — Sentence mode, and where the offline model earns its place

**Found by asking whether phrase mode had collapsed into sentence mode.**
It had not — measured as cut-point *purity*, the share of phrase
boundaries that are merely sentence boundaries, phrase mode became MORE
distinct (41.4% → 39.2%), and the shift was concentrated where the new
mechanics act (−3.2pp in paragraphs carrying `?` or `(`, −0.5pp
elsewhere).

But the reason it diverged is the finding: **sentence mode is not
cutting at those questions either.** It still requires
`[.!?]` + whitespace + CAPITAL, and this translation continues in lower
case — *"…to do me hurt? for what profit…"*. Nine such questions in
Meditations VI alone.

**Two defects, and they want different instruments.** Counted over
11,467 sentence-mode atoms from eight works:

| defect | count | fixable by |
|---|---|---|
| a `?`/`!` whose sentence continues in lower case | **61** | one line — no abbreviation ends in `?` |
| a split *after* an abbreviation (`Mr.`, `St.`, `i.e.`) | **237** | not a regex |

The false splits concentrate exactly where a reader would notice:
**Middlemarch 140, Pride and Prejudice 87** — `Mr. Casaubon` and
`Mrs. Bennet` cut in half, mid-name, on every occurrence. The capital
guard is doing abbreviation duty and doing it badly, and the fix is not
a longer abbreviation list: that list is unbounded and edition-specific.

**THIS is the task the offline model is actually for.** The awareness
study (§4) drew the line already and it holds up:

> *Clause segmentation is a parser, not a model.*

Sentence-boundary detection is the opposite case — **Punkt is literally
trained for this one job**, unsupervised, per-corpus, and it learns a
work's own abbreviations rather than being handed a list. spaCy and
stanza carry equivalents. This is the ML application with a real target.

**The hard constraint is unchanged and it decides the architecture.**
Atom boundaries are what visual programs anchor to, so a reading must be
the same reading twice. That rules out inference at read time and rules
IN what the Archive already does: derive offline, commit the artifact,
ship it. `division-index.json` is the precedent — 91 works of structure
derived once by a script and consumed by a runtime that never re-derives
it. A `sentence-index` alongside it would be the same shape.

**Order, and why:**

1. **The one-line fix first** — `?` and `!` need no capital guard, 61
   measured cases, and it can ship in an hour with the same before/after
   methodology used for 7.1. Blast radius is real though: Journeys and
   the Atrium compile at sentence level, and the Atrium pins
   compiler-derived durations, so expect the pins to move again.
2. **Then the ingest artifact** for the 237. It is a build-step, a
   committed index, and a consumer — three pieces, none of them
   speculative, and the payoff is a class of defect no constant can
   reach.

### 7.3 — The parser, for the interrupted subject

Unchanged and still open. The *interrupted subject* — 35 instances in
Book VI — did not move under 7.1 at all, exactly as the awareness study
predicted: the floor reasons about LENGTH and the defect is about
GRAMMATICAL ROLE, and punctuation cannot reach it either.

Distinct from 7.2 and worth keeping distinct: **7.2 asks where a
sentence ends, 7.3 asks what a sentence is made of.** The first is
solved by a trained boundary detector; the second needs a dependency
parse. Both run at ingest; only the second is the "parser, not a model"
the study meant.

**A verified defect, not a refinement.** `splitPhrases` splits on
`, ; : — – |` and on `.` followed by a capital. **`?` and `!` are absent
from both alternatives**, while `splitSentences` has `[.!?]` — so PHRASE
mode, the finer mode, is coarser than SENTENCE mode at a question:

```
"Who goes there? He asked again. Nobody answered."
  phrase   → ["Who goes there? He asked again.", "Nobody answered."]
  sentence → ["Who goes there?", "He asked again.", "Nobody answered."]
```

A question is the most emphatic boundary in prose and phrase mode reads
straight through it. Parenthetical clauses are never split either.

**Why this before the other open items:** it touches every reading in
the app, it is bounded, and the two prior studies
(`PHRASE-CHUNKING-STUDY`, `CHUNKER-AWARENESS-STUDY`) already establish
the measurement methodology — fragment rate, stutter runs, interrupted
subjects — so the change can be measured rather than judged by eye.

**Care required, per those studies:**
- The floor (`applyPhraseFloor`) must not merge across a `?` or `!` any
  more than it merges across a `.` — refusal #2 there covers sentence
  ends and needs to learn these.
- `!` inside a clause is not always a boundary: *"What is truth! said
  jesting Pilate"* is one utterance. Splitting after `!` unconditionally
  would fragment dialogue that sentence mode keeps whole.
- Authored `|` still wins over everything.

**Then, and only then, the ML question** (§4 of the awareness study): a
pretrained parser at ingest time, offline, deterministic. The study's own
conclusion stands — *clause segmentation is a parser, not a model*, and
determinism is a hard constraint because visual programs anchor to atom
boundaries.

---

## Phase 5 — The Solarium, narrowed

*From a product review, 2026-08-04. Summarised here; the review is the
authority on the editorial judgements of individual sequences.*

**The verdict is not to retire the Solarium. It is to narrow what it is for.**

> Keep the Solarium as RISE's temporal orchestration layer.
> Stop treating it as a separate, endlessly expanding content universe.

It answers a question nothing else in the product answers. The Chamber answers
*what do I want to read*; Journeys answer *what authored transformation do I
want to enter*; the Solarium answers **what belongs to this moment of my life**.
That relationship to lived time is genuinely unique here — most software is
temporally indifferent and serves the same thing at 7am and midnight.

### The architectural move

> **The Solarium should schedule programs, not define another species of
> program.**

It needs to contribute only:

```js
{ temporalWindow: 'evening', programId: 'inner-citadel', recurrence: 'daily' }
```

Its canonical temporal sequences become first-party Experience Programs like any
other, launchable from the Workshop, Vault, a Journey or the Chamber.

**This is why it belongs on this roadmap and not an earlier one.** The move was
expensive while the Solarium needed its own sequence format and content
pipeline; it is cheap now that `rise.experience-program.v1` is canonical and
strict. Phases 3.2 and 3.3 make the scheduled thing portable, so this lands
naturally after them.

### Keep

The doorway, the solar dial and live time context, the seven windows,
"aligned with this hour", **My Day**, assigning saved user compositions to
windows, and a small set of polished first-party temporal practices.

My Day is the most valuable part and the least developed: a window bound to a
SOL sequence *or* a Workshop blueprint *or* an Experience Program turns the
Solarium into a **recurrence and ritual system** rather than another shelf.

### Migrate

The taxonomy is three products wearing one name — seven Temporal, five
Situational, four Archetypal — and only the first is native to a solar day.

- **Temporal** stays. It is the clearest reason the feature exists, and it forms
  an actual diurnal cycle rather than a list of exercises. Edit the language to
  be more spacious and less uniformly motivational.
- **Situational** moves out of the central taxonomy into Practices or Vault
  compositions. Still schedulable through My Day.
- **Archetypal** is where the largest reduction belongs: rewrite as explicitly
  authored poetic compositions, or retire individually.
- The full catalog browser becomes **secondary** to the current recommendation
  and the personal day plan.

### Editorial review, before any of it is re-published

Four named concerns, and they matter more here than the refactor does — this is
the product speaking in its own declarative voice:

- **Microcosmic Body** blurs metaphor, spiritual reading and scientific claim
  (one spiral in galaxies, shells, storms and the cochlea; flesh and cosmos as
  "one thing"). Beautiful as poetry; as instruction it reads as unsupported
  cosmology. Either the voice changes or the claims do.
- **Cosmological Motivation** moves from scale to existential conclusion too
  fast, and carries figures that would need re-verifying as estimates change.
- **Historical Warrior** risks converting historically specific suffering into a
  general endurance metaphor.
- **Grief Space** and **After the Storm** are sensitively written, but a
  general-purpose application should be careful about implying one authored
  script answers every grief, or about drifting into a quasi-counselling role.

### The formulation worth keeping

> The Chamber is where an experience occurs.
> The Workshop is where it is composed.
> The Vault is where it is preserved.
> Journeys are where authored works are published.
> **The Solarium is when they return.**

### Stop, effective immediately

Costs nothing and can be adopted before any code moves: stop expanding the
Solarium as a parallel library of generic guided scripts, stop treating the
three categories as equally native, and stop giving every human condition a
RISE-authored motivational response.

---

## Done 2026-08-06 — three panel rulings, applied

**Gallery is the default, and it leads the list.** The presentation
buttons now read Gallery · Behind stream · Full frame, and a reader who
has expressed no preference gets Gallery. The order of a set of buttons
is a recommendation whether or not it is meant as one, and this
recommends the surface that never flashes and never goes black.

**"Across the board" means the DEFAULT, not an override.** The Chapel
asks for behind-stream and a Vault program for full-frame; the three-layer
law says a domain that authors its own surface wins, so neither was
touched. Only `ChamberOrbital`'s default moved.

**And the cortex's own default did NOT move** — that was the interesting
part. Setting `visual-cortex`'s fallback to `continuous` broke nine tests
covering the ASCII adapter route, the presentation clock and external
asset hydration, because that field is read to decide *what a flash is*.
A surface a reader picks and a surface the engine assumes are two
different constants that happened to share a value.

**ASCII is retired.** A cool experiment that did not earn its place. It is
gone from **both** surfaces where it could be chosen — the Visual panel
and the Workshop — because leaving the author's control while removing the
reader's would let a program request a surface no control can reach.
Stored configs naming it normalise to native at every boundary (panel,
compiler, app), the same migration `met-` ids got and for the same reason:
a retired option is only retired once the configs still carrying it are
answered for.

**Still open:** `ascii-engine.js` and its worker remain in the tree,
now unreachable from any UI. Deleting ~800 lines plus tests is a separate
decision from retiring the setting, and it is yours.

---

## Phase 9 — A book's own plates *(measured 2026-08-06)*

**The question was whether the figures could be found. They were never
lost — we left them behind.**

Several design and architectural texts argue *about pictures*. Dresser
writes "Fig. 27 is in the manner of an Egyptian chair"; without the chair
the sentence is a pointer into nothing. The instinct to clean these
markers away, and the counter-instinct that the texts then stop making
sense, were both right and both incomplete.

### What is actually in these texts — four shapes, three treatments

| shape | count | disposition |
|---|---|---|
| `[No. 1. LINE. Iron, XV Century]` (Dow) | 47 | caption for an absent plate |
| `[Silhouette]` (Crane) | 133 | same |
| "**Fig. 26** is essentially bad" (Dresser) | 87 | **prose — do not touch** |
| `[Greek: physiologia]` (Vitruvius) | 128 | **not an illustration; it is the word** |

The bare `[Illustration]` class was already trimmed (§2c, 205 of them);
only 3 remain corpus-wide and there is no `[Illustration NN]` at all.

### The measurement

Gutenberg **#45410** is the edition our Dow shelf already cites. We
ingested `pg45410.txt` and left `45410-h/` beside it:

- **96 images**, 83 named `no_NN.jpg`
- **27 of 28** caption numbers in our text have a matching plate
- one fetched: 200, `image/jpeg`, 34KB

Crane's edition carries **114** images, Vitruvius's **71**.

### Why this is not a new product philosophy

It is the **Audubon pattern**, which already exists twice:
`build-audubon-catalog.mjs` (585 folio plates) and
`build-science-catalog.mjs` (216 works). A work's own fixed, ordered,
rights-clear plates, harvested once by a controlled script and checked in.

**PROCEDURAL INTERPRETATION IS THE ONE WRONG ANSWER, and it is worth
stating as a rule rather than a preference.** The Chamber's procedural
imagery *accompanies*; it never claims to depict. A figure reference is
**deictic** — it points. Filling a pointer with an invention is the single
place procedural imagery becomes a lie, because a reader told "Fig. 26 is
essentially bad" and shown a generated chair believes they have seen
Dresser's chair. **Procedural imagery may accompany, never denote.**

### What is genuinely undecided

1. **Where a book's own figure appears.** Page Mode places figures inline
   and that is the obvious home. The Chamber's imagery is *accompaniment*,
   and a figure is not that — it is part of the text's argument. This is a
   presentation decision, not a harvest one.
2. **Whether Crane and Vitruvius bind as cleanly.** Dow's captions carry
   the plate number, so the binding is nominal and mechanical. Crane's are
   labels (`[Silhouette]`), so the binding may be positional — a harder
   problem, and worth measuring before promising.

---

## Phase 11 — Atrium passage additions *(2026-08-06)*

Three point launches sit at the thin end of the shelf, and the phrase
floor made that visible rather than making it true. Under honest pacing —
short atoms no longer padded by a minimum duration — they read:

| launch | words | duration | words/min |
|---|---|---|---|
| `point-hist-seven-years-war` | **371** | 2.97m | 125 |
| `point-hist-first-continental-congress` | 400 | 2.99m | 134 |
| `point-hist-treaty-paris-1763` | 405 | 3.00m | 135 |
| *median launch* | *544* | *4.07m* | *134* |

Words per minute is flat across the whole band, so **none of these got
thin; they were always the shortest texts on the shelf.** The duration
floor had been clearing them on padding.

`seven-years-war` is the one worth acting on: **371 words against a
544-word median**, and a single passage — `pass-franklin-war-finance-1766`
— carrying an anchor as consequential as the fiscal origin of the
imperial crisis. A second unit would serve the argument, not just the
clock. The other two are within 45 words of their neighbours and want
nothing.

**This is an acquisition, not an edit** — the same class as the
Mahabharata's true volumes. It needs a source chosen by hand, audited,
and added to the pilot pack, which is why it is recorded rather than done.

**The guard that will notice the next one** is already in place and is on
WORDS, not on time: `THIN_LAUNCH_WORDS = 350` in `launches.test.js`, set
below the shortest current launch so it fails on a new thin launch rather
than re-litigating these. A duration floor was only ever a proxy for
substance, and it moved twice in one day; word count does not move when
the chunker learns something.

---

## Phase 10 — Apparatus in the reading stream — **A and B APPLIED 2026-08-06**

**49 divisions withdrawn (22,086w), 13 imprints trimmed (295w), and
1,844,350 words of genuine reading kept.** `scripts/withdraw-apparatus.mjs`
reports by default and writes only with `--apply`; re-run afterwards it
reports 0 for class A, so the pass converged.

The detector was wrong three times before it was right, and each
correction is in its source:

- **A publisher's name is not an imprint.** Matching `Trübner|Longmans|
  Macmillan` caught Burton's endnote — *"The Arabian Nights'
  Entertainments (London: Longmans, 1811) by Jonathan Scott"* — and would
  have trimmed 42 words of scholarship. Set phrases only.
- **Nor is the phrase, wherever it falls.** `the-no-plays-of-japan
  §CHAPTER VI` is a genuine note on two Nō plays containing *"though
  printed by both Ōwada and Haga"*. What marks a colophon is its
  TYPESETTING — set short and alone, where prose wraps to the measure.
- **And tightening that threshold too far broke it the other way.** At 45
  characters nine of Burton's ten title pages vanished: a false negative
  produced by the fix for a false positive. 60, with both boundary cases
  named in the source so a later reader can judge it.

### What was deliberately NOT cut

**15 divisions carry a contents block and are flagged for a human**, because
the name "Front matter" holds a title page in `the-ramayan-of-valmiki` and
a critical introduction in `crime-and-punishment` — *"Dostoevsky was the
son of a doctor…"*, a reading somebody may want. A name that means
opposite things in two works is not evidence. The list includes
`middlemarch`, `moby-dick`, `the-iliad`, `war-and-peace`, `the-odyssey`,
and `a-doll-s-house §ACT I.` at 47 words.

**And `sacred-i-ching §Full text` is one division of 170,390 words** — a
whole scanned book, title page and contents and preface inside a single
reading. One imprint block was trimmed; the rest needs a human, because
chewing through it paragraph by paragraph would be uncontrolled. That it
is undivided at all is a separate defect from the apparatus in it.

### Still open

The **per-division identity guard** (§1b) remains unbuilt and remains the
thing that would stop the next one being found by accident: *no division
may be devoid of the vocabulary its work is dense in*, and its converse —
no division should be dense in the vocabulary of a printer.

---

## The original measurement *(2026-08-06)*

Two divisions turned up **by accident** while sampling for the floor
study: the Ramayan serves a title page and `a-hundred-verses-from-old-japan`
serves an INDEX. Nothing was looking for them. Sized afterwards across all
91 works and 7,670 divisions, and it is **two defects, not one** — a single
total would hide both.

### A · Divisions that ARE apparatus — 84 divisions, 6 works

Index, glossary, contents. **35,793 words, median 356w.** Small and wholly
removable; the reading loses nothing because none of it was a reading.
The Shahnama alone carries **71** (`Volume 1 — INDEX` and its siblings) —
the same work whose 462 sections resolve to 249 distinct names, which is
how a keying bug hid there earlier.

`the-troubadours §CHAPTER IX` is the one to look at first: **174 of its
205 lines are index-shaped**, under a chapter's name. Named as a reading,
built as an index.

### B · An imprint at the head of a genuine division — 20 divisions, 8 works

`printed by`, `published by`, `Trübner & Co.` at the top of a real
volume. These readings are **real** — Thousand Nights Volume I is 177,675
words of Burton — so this is a **trim of the opening**, never a
withdrawal. Quoting the 1.85M words these divisions contain would badly
misstate the work.

### Why this is the same failure the Mahabharata was

Identity was asserted of the **work** and served at the **division**. A
work passes every check it has while individual divisions are something
else — a war periodical there, a printer's colophon here. §1b's suggested
guard is still unbuilt and still the right one: *no division may be devoid
of the vocabulary its work is dense in*, plus its converse — no division
should be dense in the vocabulary of a **printer**.

### Order of work

1. **Class A**, by detector plus review. Small, evidenced, and the
   Shahnama's 71 are one pattern seen 71 times.
2. **Class B**, trimming only the preamble, with the division's own first
   sentence as the boundary evidence.
3. **The per-division identity guard**, so the next one is not found by
   accident while measuring something else.

**Still open from the earlier pass** and unchanged: the Shakespeare
edition (blocks `re-source`), the Mahabharata's true volumes 5–18 (the
trim was the interim), ~244 shelved orphan-`]` lines in 12 works, and
decision 0's stranded running heads.

---

## Phase 8 — The Vertically Responsive Text Band *(Mateo, 2026-08-06)*

> **SHIPPED 2026-08-10** (`40ee391`). The four open questions below were
> answered by ruling rather than by design, and the answers narrowed the work
> to roughly what the transform always was.
>
> **The band is a PREFERENCE, not identity.** No authored field, no Experience
> Program track — "left to users for now". That answers the ownership question
> the section raises and removes the three-layer law from the problem entirely:
> nothing authors the band, so nothing can be silently overruled. It persists
> as `bandOffset` through `handleSettingsChange`, validated like every other
> preference.
>
> **Held as a FRACTION of available travel, not pixels.** The same reader meets
> the same reading on a phone and a monitor, and "a third of the way up"
> survives that change where "160px" does not. `bandTravelPx` is half the room
> left when the band's own height is taken out, floored at zero — a negative
> travel would invert the drag.
>
> **Drag only, and no control-bar entry on either surface.** On a phone the
> band is dragged directly. On desktop it is **click to select** — a light blue
> frame — **then** drag. The gesture is disambiguated from advancing the
> reading by a 4px threshold with pointer capture, which is the part the
> section correctly identified as the hard bit rather than the transform.
>
> **The phone takes the transform on `.atom-band`, desktop on `.atom-display`
> — never both.** Compounding the two would slide the text out of its own tile.
>
> **Page Mode is untouched**, per the section's last bullet: the setting
> governs the stream, which is the only projection with a band.
>
> **Open, and small:** travel is currently the full field, so the band can be
> dragged down onto the control bar and may want to stop short of it; and
> `outline-offset: 14px` is tuned for a monitor and may read as a stray
> floating frame on a phone.

**The report.** On a phone, in a Journey, the reading text sits over the
centre of the screen — which is exactly where the visualisers put their
subject. The two are fighting for the same pixels and the text wins,
because it must.

**The proposal, as given:** make the band's y-position **programmable**
(desktop and mobile), and let a reader **drag it up or down**.

**Why it is not just a CSS offset.** The band is centred by flexbox —
`.chamber-field` is `flex: 1` with `align-items: center` — so the
mechanism is one transform. Everything expensive is around it:

- **Programmable means authored**, and authored means the three-layer law
  applies: a content domain sets the band, the runtime follows, the cortex
  renders. That is a field on the Experience Program, not a preference —
  and a Journey that positions its band is making the same kind of
  statement as one that positions its imagery.
- **A reader's drag must not overrule an authored band silently**, nor be
  forgotten the moment they do it. This is the same ownership question the
  `visualProgram` persistence bug turned out to be: text identity vs
  preference. Decide which one the band is BEFORE building it.
- **The gesture has to be disambiguated from advancing the reading.** The
  Chamber's field responds to input already; a drag that starts as a tap
  is the hard part, not the transform.
- **Page Mode has no band** — it has a column. The setting should either
  be projection-scoped or say plainly that it governs the stream only.

**Worth noting it is a mobile fix with a desktop life.** Even on a wide
screen, a reader who wants the imagery uncovered has no way to ask.

---

## Phase 6 — The Atrium on a phone *(Mateo, 2026-08-05)*

**The Atrium needs heavy UI optimisation on mobile.** Sequenced beside the
Solarium because both are surfaces the mobile density pass never reached, and
because doing them together means measuring the phone once.

**It is the least mobile-adapted surface in the app, and the numbers say so:**

| surface | media rules | lines | mobile pass? |
|---|---|---|---|
| Chamber | 9 | 1,370 | yes, 2026-08-04 |
| Portal | 7 | 1,420 | yes |
| Library | 5 | 915 | partial |
| **Atrium** | **3** | 1,233 | **no** |

**And its layout is the hard case, not a spacing problem.** The Atrium places
works at computed absolute coordinates on a horizontal era timeline —
`x: 42 + eraIndex * 250, y: 72 + row * 92`. That geometry assumes a desktop
canvas; on a 390px viewport an era is two thirds of a screen wide and the
timeline is a scroll the reader cannot see the shape of. **Tightening the CSS
will not fix a coordinate system.** The question to answer first is what the
Atrium *is* on a phone — the same timeline scrolled, or a different projection
of the same collection, in the way the Page is a projection of the Stream.

**Method, per the mobile pass that worked:** measure before changing anything.
That pass moved the Vault from 1 archetype visible to 5, the Library from 0
books on first screen to 1, and the Audio panel from 1,273px to 919px — by
counting what reached the first viewport, not by judging screenshots. The
standing note in *Parked* applies in reverse here too: **the desktop numbers do
not transfer.**

**Constraint, unchanged:** `aic-*` category ids stay untouched, and the Atrium
is curation-only — every image is a work someone chose. A density pass may
change what is shown first; it may not change what is shown.

---

## Parked, with the reason

| item | why parked |
|---|---|
| **Science imagery** — `docs/vision/SOURCE-EXPANSION-SPEC.md` | **COMPLETE AND DEPLOYED 2026-08-05.** All three §3a gaps closed; licence classes, the withholding guard, the roster ruling and the Curia record all shipped. 216 astronomy works pinned and served through `Science Collections`. Smithsonian reviewed entire and retired (54 refusals recorded). Remaining, and small: nothing selects Astronomy automatically — a cosmology reading still needs it ticked by hand (§4 route 1). Rights are FROZEN at harvest with no staleness signal, so `build-science-catalog.mjs` wants re-running periodically. ESA/Hubble and ESO are exhausted to their 100-per-feed pagination ceiling. |
| **Spatial Chamber** — `docs/vision/SPATIAL-CHAMBER-SPEC.md` | Specced, unbuilt, self-contained. No dependency either way. |
| **Text-Attuned Imagery** — `docs/vision/TEXT-ATTUNED-IMAGERY-SPEC.md` | Specced, unbuilt. Worth re-reading against the now-canonical Experience Program before building — the source-span work may have changed what it should be. |
| **Rijksmuseum enrichment** | Explicitly gated on the Chapel's seven stages completing. Also touches a configured personal vault, so contact-sheet review before anything ships. |
| **Three withheld Shakespeares** | Blocked on a decision, not on work — see below. |
| **War's three soundscapes** | `war-ordered-field`, `war-mortal-pulse`, `war-pressure-field`. Composition work, not engineering; War degrades correctly to silence and says so in `openRequirements`. Schedule when there is appetite to compose rather than build. |
| **Four remaining Milton figures** | `docs/specs/BOOK-VI-PROCEDURAL-WORKS.md`. Independent; good filler work. |
| **Desktop density audit (1440/1920)** | Agreed after the mobile pass. Measure before changing anything — the mobile numbers do not transfer. |
| **Page dynamic score projection** — cue-specific Genesis/Attractor samples and MP4 posters | Static Workshop visual parity is complete. Multiple field configurations need an abortable config-keyed sampler with bounded caching/sample budget; video needs deterministic poster extraction/storage. Keep both out of collection ids and out of Page's no-clock renderer until that contract is designed. |
| **Stacked-toast overlap** | Two toasts render on top of each other mid-screen. Cosmetic, real, small. |
| **`viewport-fit=cover`** | Deliberately parked with reasoning recorded in `Portal.css`. Revisit only if the Chamber's imagery should go genuinely edge-to-edge. |
| **Dreams** — `docs/vision/DREAMS.md` | **Not parked, not scheduled, not a phase.** Unsequenced experiments recorded so they are not lost: the RISE Familiar and Civitas. Listed here only so the file is findable from the document that is read first. |

---

## Decisions needed from Mateo

1. **Shakespeare edition.** Three works are off the shelf and the shelf says why.
   Re-sourcing needs an edition chosen first; the Moby Shakespeare is the
   recommendation.
2. **Phase 4 ordering.** Finish Phase 3 first, or jump to RISE Chain after 3.4?
3. **Are the War soundscapes in scope this session,** or does War keep reading in
   silence a while longer?
4. **Stranded running heads** — a bounded deletion with the text's own
   duplication as evidence. See `ARCHIVE-CLEANSING-SPEC` decision 0. Flagged
   rather than fixed, because removing text is your call.

---

## A property worth knowing before the next session

**A reading's text is persisted when it is chosen, not derived when it is
read.** So a fix to how divisions are cut does not reach a reading already in
hand — a hard reload will not do it; the text has to be cleared and re-added.
That cost real confusion today, on a fix that was already correct and already
verified. It is not obviously a bug (a reader's text should be stable), but it
means *any* archive-side correction — the cleansing pass above especially —
lands only for readings chosen after it ships.

---

## Phase 12 — Two external reviews, run down *(2026-08-06)*

Two reviews of the same tranche arrived within a day of each other: GPT on
`36f8232`→`0c3e192`, SOL on `0c3e192`→`84d1ab8`. Both are recorded here with
what verification actually found, because in three cases the finding was right
and the prescribed fix would have caused an outage.

### Closed in `2630bb2` and the commit after it

| finding | who | what verification found |
|---|---|---|
| `fullCredit` truncated at 500 chars | both | **CONFIRMED.** The cap ran before the assignment, so the Curia's "full record" — the thing that makes roster elision lawful under CC BY 4.0 §3(a)(3) — was false for exactly the credits long enough to need eliding. One in the corpus is 723 characters. Uncapped. |
| Unrecognised rights read as open | both | **CONFIRMED AND WORSE.** `CC BY-NC 4.0` satisfied the CC-BY test and came out as plain `cc-by`, dropping NonCommercial entirely. Restriction is now tested first. |
| Roman numeral pattern matches empty string | GPT | **CONFIRMED.** Every group optional, so a bare `.` was accepted as numeral-plus-terminator. |
| Nine permission-granted icons owed an unenforced credit | *neither* | Found while checking the above. `rights: 'PERMISSION'` classified as OPEN; the Icon Museum's stated condition held only because an attribution string happened to sit beside it. |
| `CC BY-ND` still passing as attribution | *neither* | Found by the test written for the NC fix. A bare `nd` is not evidence; `CC BY-ND` is. |
| Boundary between Apache code and reserved content | SOL | **CONFIRMED.** `ASSET-LICENSES.md` licensed all program code under `src/` and separately reserved `src/visuals/` — 41 files of engines. Rewritten to follow the bytes, with a stated rule for modules holding both. The claim over "procedural visual systems as compositions" was **dropped**: a grant to run and modify an engine is a grant to what it draws. |
| Stale "phrase floor is opt-in" comment | SOL | Confirmed and rewritten. The two carve-outs it justified — speaker heads, verse lines — survive for their own reason. |
| "these nine" exemptions | SOL | Confirmed; there are **ten**. Re-measuring the block's other number found a whole-payload search rescues **eight**, not six. Both corrected. |
| `NOTICE` names a retired Smithsonian source; claims per-work detail | SOL | Both confirmed and corrected. |
| Stale "closed beta" in the invite fallback | SOL | Confirmed. Any invite without its own `welcome` contradicted the footer. |

### The finding that inverted on inspection

**Unknown declared rights should fail closed** (SOL §4) was right about the
defect and, implemented as written, would have withheld nearly the entire
visual corpus.

`OPEN_RIGHTS` was `/public[\s-]*domain/` and the corpus's commonest rights
string is `PUBLIC_DOMAIN` — an underscore is neither whitespace nor a hyphen.
It never matched. All 585 Audubon plates and the value the museum adapter
normalises all 1,699 museum pins to were classified `open` **on the permissive
fallback**: the right answer by the wrong route, invisible for exactly as long
as the fallback stayed permissive.

So the recogniser was widened first and the fallback closed second. All eight
rights strings the corpus declares are asserted, by name, in
`artwork-label.test.js`. **The order was the whole of the work; the diff is
four lines.**

> Before changing a classifier that fails closed, enumerate what it must
> recognise. A guard added to a vocabulary that does not cover the corpus is
> an outage with a good rationale.

### Now scheduled

**12.1 — CI.** *(shipped this session)* `.github/workflows/ci.yml`: unit,
build, hygiene, browser smoke. `scripts/ci-hygiene.mjs` checks the committed
catalogues rather than the code that reads them — no secret in a delivery URL,
no secret-shaped `VITE_` variable reaching the client bundle, every work owing
a credit has one, no catalogue declares rights the classifier cannot read. Each
of the four was verified to fail by planting the defect it looks for.

The argument for it is this session: a 128 ms drift in one of 101 pinned Atrium
durations, caught only because someone typed the command.

**12.2 — The Chain tokenizer is ASCII-only.** `src/chain/markov.js:74` is
`/[A-Za-z][A-Za-z'’-]*|[.,;:!?—–]/g`. Greek, Cyrillic, Devanagari, Arabic and
CJK yield **zero tokens**; that part is silent absence and tolerable. The
actual failure is accented Latin, which is **shredded**: `größere` → `gr`,
`ere`; `côté` → `c`, `t`; `Straßen` → `Stra`, `en`. Those are not dropped, they
enter the chain as tokens and get emitted as words. One translation with a
transliterated name is enough.

Fix with Unicode property escapes, and add the corpus-retention report both
reviews asked for — the failure is invisible without one.

*Priority note: `src/chain/markov.js` is imported by nothing but its own test.
It is Phase 4 built ahead of its UI. Fix before the lane ships, not after.*

**12.3 — `maxVerbatim` is a token bound named like a string bound.** Same
file, same unreachability. Rename, or add the rolling n-gram overlap guard.
Cheapest while nothing depends on it.

**12.4 — Per-division vocabulary identity.** `ARCHIVE-CLEANSING-SPEC` §1b.
The opening-title guard now carries ten exemptions because legitimate books
stopped naming themselves once title pages were withdrawn. Widening the window
rescues eight of them and is refused: a test that recovers almost everything by
looking almost everywhere has stopped asking a question. **Build this before
the next corpus expansion.**

**12.5 — Custom domain.** The public URL still carries
`rise-v2-symbolic-experience`, an identity two renames old.

### Recorded, not scheduled

- `BetaGate` persists a session only for codes resolving through
  `BETA_INVITES`, so an anonymous `"open"` visitor meets the threshold again
  after a reload. Arguably correct — the entrance click is also the audio
  gesture — but the class comment's "session persistence" is no longer true
  for everyone. Comment, not behaviour.
- Apache 2.0 does not stop a closed commercial derivative. Accepted knowingly;
  `NOTICE` is informational and does not modify the licence.
- Sextus's payload carries transcription damage — `imperturbability, . We` and
  `things () opposed`, where Greek terms were stripped. Surfaced by a 128 ms
  duration drift. The payload is checksummed and audited, so correcting it is
  an editorial act needing a ruling, not a fix.

---

## Phase 13 — A program that can score the reading *(decided 2026-08-08)*

> **SHIPPED 2026-08-10.** The three open questions below were ruled on and
> the answers are recorded here, because the ruling is the part a later
> session cannot reconstruct from the diff.
>
> **1. A reading TRACK, not a field on movements.** `kind: 'reading'`, cue
> `{ kind: 'pace', wpm?, chunkMode? }`, at least one of the two. The payoff
> was immediate and free: same-lane exclusivity already loops over every
> track kind that is not movement or transition, so two pace cues over one
> span are refused with no new code, and array order never became a mix law.
>
> **2. A scored pace is a DEFAULT.** Expressed two ways. The track carries
> **no fallback** — outside a scored span the reader's own pace governs, and
> a fallback there would be a second authority over the whole reading. And
> pace is baked into atom durations at compile time, so the Chamber's
> existing speed control keeps scaling the whole reading uniformly: the
> reader sets the tempo, the score keeps its contour.
>
> **3. The Atrium's 101 pins do not move**, per the ruling, and by
> construction rather than by tolerance: no Atrium launch authors a reading
> track, and with no track the compiler makes byte-for-byte the call it
> always made. A test asserts the unscored path is identical.
>
> **THE ONE RULE WORTH CARRYING FORWARD.** A scored `chunkMode` may not use
> a **progress** anchor. Progress is a fraction OF the atom stream, and a
> chunk mode decides how that stream is cut — so a progress range cannot
> locate the cut it is asking to change. Character, token and quotation
> anchors resolve against the raw edition text and are fine; an unranged
> clip is the whole source and is fine. `wpm` has no such limit, because
> retiming does not move a cut. Refused at the canonical boundary as
> `PROGRAM_READING_CHUNK_ANCHOR`, with a copyable explanation.
>
> **A chunk profile refuses to be cut.** The scripture profile numbers
> verses across the whole text and the dialogue profile promotes speaker
> heads, so a source carrying one cannot also carry a pace that VARIES
> within it — each side would be prepared as if the other did not exist.
> A pace over the whole source is still available. Refused, not approximated.
>
> **Found by the suite, worth keeping:** the worked example in the curator
> prompt was itself invalid — it put a progress clip and a quotation clip on
> one source, which exclusivity refuses because two ranged clips in
> different coordinate systems cannot be shown not to overlap. The prompt
> now states that constraint rather than demonstrating a violation of it.

**RULED: an Experience Program should be able to schedule pace.**

`experience-program.js` contains no `wpm`, no `chunkMode`, no `curve`.
The canonical program schedules movements, transitions, visuals, audio
and swells; pace lives at the project level as one setting for the whole
reading. So an authored score can arrange imagery and sound over a text
and cannot touch how the text moves — which is the one thing RISE does
that nothing else does.

The consequence is largest for the Live Curator: a model handed "a
sequence about memory and loss" is currently an imagery-and-sound
arranger. Composing means being able to slow into a passage, phrase-chunk
one section and word-chunk another, hold at a line.

Open before building:

- **A reading track, or a field on movements?** A track is consistent
  with the lane model and gets same-lane exclusivity free. A field is
  smaller and cannot express a change mid-movement.
- **The three-layer law applies.** A reader's WPM control and an authored
  pace are the same ownership question `visualProgram` persistence turned
  out to be. Decide whether a scored pace is a floor, a default, or
  absolute.
- **`estimateCompiledDuration` reads a single wpm.** A scored pace makes
  duration a function of the program, and 101 pinned Atrium durations
  depend on the current answer.

### Also open, from the same review

**Nineteen engines have no honest description anywhere. — DONE.** All
nineteen carry one now: 13 in `WORK_ENGINE_MANIFEST` and 6 in
`PROCEDURAL_PATTERNS`, both surfaced through `buildCuratorContext`. They
say whether a field is dense or sparse, still or moving, figurative or
abstract — which cannot be derived and had to be written.

**`category` was pulled from the exported catalogue.** All six Paradise
engines share one value and five of Storm's seven are singletons: a
display heading, uniform where it would need to discriminate and unique
where it would need to group. It remains in WORK_ENGINE_MANIFEST, which
mirrors the engine modules and is checked against them.

**`collections` holds four kinds of thing** — museum categories,
procedural pools, work-engine families, sequence assets — which is why
`klee` appears in both `collections` and `engines` and why the catalogue
needs a `kind` field to explain it. A product-modelling seam the curator
context exposed rather than created.

**The Scriptorium door is open (additive).** Prompt export, Library catalogue
in context, quotation anchors, and the Portal door beside the Curia ship.
Workshop curator buttons remain until the room is proven by a hand-run loop.
`docs/vision/SCRIPTORIUM-SPEC.md`.

**The room is a composer now, not a decorator.** Pace was the last lane a
model could not reach, and it is the one nothing else in the product offers.
A curator handed the catalogue, the engine descriptions and quotation anchors
can now also slow into a passage, hold a section in whole phrases, and quicken
a narrative stretch — and the prompt says so, with its one restriction stated.

**Still unproven by a hand-run loop.** The boundary, the refusals and the
compile path all have tests; what has not happened is a real model being
handed a real context and its score being read end to end. That is the next
thing this room needs, and it is an afternoon, not a phase.
